---
trigger: always_on
---

# New plugin scaffold

When creating a **new plugin**, use the Quan ERP CLI. Do not copy another plugin directory.

```bash
quan-erp new
# or: quan-erp new-plugin
```

Then implement the requested features inside `plugins/<name>/`.
